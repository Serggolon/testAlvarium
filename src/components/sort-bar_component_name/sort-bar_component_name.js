import React from 'react';

import './sort-bar_component_name.css';

const SortBarComponentName = ({name}) => {

    return (
        <p className={"sort-bar_component_name text-bold"}>
            {name}
        </p>
    );
};

export default SortBarComponentName;